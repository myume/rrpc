[package]
name = "rrpc-macros"
version = "0.1.0"
edition = "2024"

[lib]
proc-macro = true

[dependencies]
rrpc-core = { path = "../rrpc-core" }

quote = "1.0"
syn = { version = "2.0",  features = ["full"]}
